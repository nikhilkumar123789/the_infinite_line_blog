{
  "name": "the-infinite-line-blog",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify",
    "start": "hugo server"
  }
}
